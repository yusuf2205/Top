// Design system placeholder — real components (Button, DataTable, QuoteComparison,
// SupplierScore, ApprovalTimeline, ...) are built starting M2 alongside the screens
// that need them (see ARCHITECTURE.md §102). M0 only proves the workspace wires up.
export const UI_PACKAGE_PLACEHOLDER = true;
